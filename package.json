{
  "name": "@domien-sev/agent-research",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Research agent — web search, data gathering, analysis, and summarization",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@domien-sev/agent-sdk": "*",
    "@domien-sev/directus-sdk": "*",
    "@domien-sev/shared-types": "*",
    "@directus/sdk": "^18.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "@types/node": "^22.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
